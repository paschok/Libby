<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="paulryan.libby">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".books.AdventuresActivity" />
        <activity android:name=".books.ClassicsActivity"/>
        <activity android:name=".books.DetectivesActivity"/>
        <activity android:name=".books.HorrorActivity"/>
        <activity android:name=".books.FantasyActivity"/>
        <activity android:name=".books.SciFiActivity"/>
        <activity android:name=".books.ScienceActivity"/>
        <activity android:name=".books.PoemsActivity"/>
    </application>

</manifest>